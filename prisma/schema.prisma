generator client {
  provider   = "prisma-client"
  output     = "./generated"
  engineType = "node"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model TestProfile {
  id   String @id @default(uuid())
  name String
}
